# Keep the real file at /etc/xia-router-backup-monitor.env on PHX-ARM, mode 600.
# Use a private Uptime Kuma URL or a Cloudflare Access service token.
KUMA_PUSH_URL='http://UPTIME-KUMA-PRIVATE-IP:PORT/api/push/REPLACE_WITH_NEW_MONITOR_TOKEN'
