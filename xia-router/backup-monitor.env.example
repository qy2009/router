# Keep the real file at /etc/xia-router-backup-monitor.env on PHX-ARM, mode 600.
# phx-casa keeps Kuma bound to 127.0.0.1; PHX-ARM reaches it through Tailscale SSH.
KUMA_PUSH_URL='http://127.0.0.1:3001/api/push/REPLACE_WITH_NEW_MONITOR_TOKEN'
KUMA_PUSH_SSH_HOST='phx-casa'
